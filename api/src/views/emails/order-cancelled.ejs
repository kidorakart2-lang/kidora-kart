<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Order #<%= order.orderId %> Cancellation Confirmation</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        text-align: center;
        padding: 20px 0;
        border-bottom: 2px solid #f0f0f0;
        margin-bottom: 30px;
      }
      .logo {
        max-width: 200px;
        margin-bottom: 20px;
      }
      .order-details {
        background: #f9f9f9;
        padding: 20px;
        border-radius: 5px;
        margin: 20px 0;
      }
      .refund-details {
        background: #fff8f8;
        border-left: 4px solid #e74c3c;
        padding: 15px;
        margin: 20px 0;
      }
      .button {
        display: inline-block;
        padding: 12px 24px;
        background-color: #d4af37;
        color: #fff !important;
        text-decoration: none;
        border-radius: 4px;
        margin: 20px 0;
        font-weight: bold;
      }
      .footer {
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px solid #f0f0f0;
        font-size: 12px;
        color: #777;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Order #<%= order.orderId %> Cancelled</h1>
    </div>

    <p>Dear <%= user.name %>,</p>
    <% if (order.adminReason) { %>
    <p>
      We Are Sorry to say That we had to cancel your order Because
      <strong>Reason:</strong> <%= order.adminReason %>
    </p>
    <% } %> <% if (order.paymentRefundStatus) { %>
    <p>
      <strong>Your Payment Refund Has Started Processing</strong>
    </p>
    <% } %>
    <p>
      We've processed the cancellation of your order
      <strong>#<%= order.orderId %></strong> as requested on
      <strong><%= new Date().toLocaleDateString() %></strong>.
    </p>

    <% if (!order.pendingStatus) { %>
    <div class="refund-details">
      <h3>Refund Information</h3>
      <p>
        <strong>Refund Status:</strong>
        <% if (order.cancellation.refundStatus === 'initiated') { %> Your refund
        of <strong>₹<%= (order.pricing.total / 100).toFixed(2) %></strong> has
        been initiated. <% } else if (order.cancellation.refundStatus ===
        'failed') { %> We encountered an issue processing your refund. Our team
        has been notified and will process it manually. <% } else { %> Your
        refund will be processed within 5-7 business days. <% } %>
      </p>
      <% if (order.cancellation.refundId) { %>
      <p><strong>Refund ID:</strong> <%= order.cancellation.refundId %></p>
      <% } %>
      <p>The refund will be credited back to your original payment method.</p>
    </div>
    <% } %>

    <div class="order-details">
      <h3>Cancelled Order Details:</h3>
      <p><strong>Order Number:</strong> #<%= order.orderId %></p>
      <p>
        <strong>Order Date:</strong> <%= new
        Date(order.createdAt).toLocaleDateString() %>
      </p>
      <p>
        <strong>Order Total:</strong> ₹<%= (order.pricing.total ).toFixed(2) %>
      </p>
    </div>

    <p>
      If you didn't request this cancellation or have any questions, please
      contact our support team immediately.
    </p>

    <div style="text-align: center">
      <a
        href="<%= process.env.FRONTEND_URL || 'https://yourwebsite.com' %>/contact-us"
        class="button"
        >Contact Support</a
      >
    </div>

    <p>We're sorry to see you go. We hope to serve you again in the future.</p>

    <div class="footer">
      <p>
        © <%= new Date().getFullYear() %> Jewellry Wala. All rights reserved.
      </p>
    </div>
  </body>
</html>
