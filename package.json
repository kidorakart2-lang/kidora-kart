{
  "name": "toy-shop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,admin,web -c blue,green,yellow \"pnpm --filter api run dev\" \"pnpm --filter admin-panel run dev\" \"pnpm --filter web run dev\"",
    "build": "pnpm --filter api run build && pnpm --filter admin-panel run build && pnpm --filter web run build",
    "build:api": "pnpm --filter api run build",
    "build:admin": "pnpm --filter admin-panel run build",
    "build:web": "pnpm --filter web run build",
    "typecheck": "pnpm --filter api run typecheck && pnpm --filter admin-panel run typecheck",
    "lint": "pnpm --filter api run lint && pnpm --filter admin-panel run lint"
  }
}
